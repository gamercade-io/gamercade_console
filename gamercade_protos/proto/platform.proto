syntax = "proto3";

import "common.proto";

package platform;

service PlatformService {
    rpc GetHomeView(common.Empty) returns (HomeView);
}

message HomeView {
    repeated GameEntry game_entries = 1;
}

message GameEntry {
    uint32 game_id = 1;
}
